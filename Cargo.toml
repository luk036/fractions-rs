# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "fractions-rs"
version = "0.1.3"
authors = ["Wai-Shing Luk"]
exclude = [
    "/bors.toml",
    "/ci/*",
    "/.github/*",
]
description = "Fractions numbers implementation for Rust"
homepage = "https://luk036.github.io/fractions-rs"
documentation = "https://docs.rs/fractions-rs"
readme = "README.md"
keywords = [
    "mathematics",
    "numerics",
    "fractions",
]
categories = [
    "algorithms",
    "data-structures",
    "science",
    "no-std",
]
license = "MIT OR Apache-2.0"
repository = "https://github.com/luk036/fractions-rs"

[lib]
name = "fractions"
path = "src/lib.rs"

[package.metadata.docs.rs]
features = [
    "std",
    "num-bigint-std",
    "serde",
]

[dependencies.num-bigint]
version = "0.4.5"
optional = true
default-features = false

[dependencies.num-integer]
version = "0.1.46"
features = ["i128"]
default-features = false

[dependencies.num-traits]
version = "0.2.19"
features = ["i128"]
default-features = false

[dependencies.serde]
version = "1.0.200"
optional = true
default-features = false

[build-dependencies.autocfg]
version = "1.2.0"

[features]
default = [
    "num-bigint-std",
    "std",
]
num-bigint-std = ["num-bigint/std"]
std = [
    "num-integer/std",
    "num-traits/std",
]

[dev-dependencies]
quickcheck = "1"
quickcheck_macros = "1"
